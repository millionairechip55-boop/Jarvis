/**
 * This file is intentionally left with empty functions to disable audio feedback sounds
 * as per the user's request to "remove the update" that added them.
 */

export const playMicOnSound = () => {
  // Do nothing. Sound disabled.
};

export const playMicOffSound = () => {
  // Do nothing. Sound disabled.
};
